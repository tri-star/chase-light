# 作業ログ 2025-11-26 セッション01

## 対応概要
- 課題: CHASE-158
- SOW: docs/sow/20251126-CHASE-158.md
- フェーズ: Activity詳細ページ実装中（BFF/UIは追加済み）

## 現在の問題
- フロントエンドのVitest実行で `components/pages/activities/detail/__tests__/activity-detail.test.ts` が失敗。
- 原因: テストで`/activities/[id].vue`を`mountSuspended`する際にルートパラメータ`id`が正しく渡らず、`useAsyncData`が`/api/activities/undefined`を叩いて404となりタイムアウトしている。
- 状態: ルートパラメータのモック/セットアップ方法を見直す必要あり。テスト以外の実装・他テストは通過。

## 次のアクション候補
1. `mountSuspended`の呼び出し時にルート名・paramsをNuxtの期待通りに設定する（例: `{ name: 'activities-id', params: { id: 'activity-1' } }` の正確な指定を確認）。
2. もしくはページ内で`useRoute`をテスト用にモックする/`provideRoute`を使う等で`id`を強制付与する。
3. 修正後に`pnpm --filter frontend test -- components/pages/activities/detail/__tests__/activity-detail.test.ts --runInBand`で再確認。
