{
  "name": "@chase-light/infra-api",
  "version": "1.0.0",
  "private": true,
  "description": "SAM Infrastructure for Chase Light API",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy",
    "deploy:guided": "sam deploy --guided",
    "validate": "sam validate",
    "local:start-api": "sam local start-api",
    "local:start-lambda": "sam local start-lambda",
    "sync": "sam sync --stack-name chase-light-sam",
    "delete": "sam delete"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}