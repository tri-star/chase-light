import { describe } from "vitest"

// NOTE: このファイルはIdentity再構成後のテスト移行のために残しています。
// 新しいルートテストは `features/identity/presentation/routes/auth/__tests__/index.test.ts` を参照してください。
// 移行完了後に削除予定です。

describe.skip("legacy auth route specs", () => {})
